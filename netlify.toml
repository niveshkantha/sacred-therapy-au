[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

# Netlify auto-installs @netlify/plugin-nextjs when it detects Next.js.
# API routes (e.g. /api/begin) run as serverless functions via that runtime.
